{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "buildCommand": "npx prisma generate && npm run build",
    "startCommand": "sh -c 'npx prisma migrate deploy || echo \"[WARN] prisma migrate deploy failed, continuing startup\"; npm run start:prod'"
  }
}
